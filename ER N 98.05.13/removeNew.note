	// LRT - 97/05/22 2:48 AM	// Remove all old "_new" items before beginning	// a receive session.	// NEW:	local iSoup := GetUnionSoupAlways ( k_infoSoupName );	local sCursor;	local e;	local switchQuery :=		{		tagSpec:	{ all: '_new },		};	while ( e := (iSoup:Query ( switchQuery )):Entry() ) do		begin		if kDebugOn then print ( "- removed '_new  id:" && e.id );		EntryRemoveFromSoupXmit ( e, nil );		end;	// END OF NEW: 97/05/22 2:48 AMstats();